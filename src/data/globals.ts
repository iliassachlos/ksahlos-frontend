import { alpha } from "@mui/material";

export const HEADER_HEIGHT = 76;
export const SIDEBAR_WIDTH = 270;
export const HOME_TOP_OFFSET = HEADER_HEIGHT + 64; // header height + HomeView's top margin

export const HERO_OVERLAY_COLOR = alpha("#08070a", 0.35);
